package com.tech.subash.memory;

/*
  STACK :: Method calls, local vars, references,
           Static (at compile-time),
           JVM (automatically on call/return)
           One per thread
  HEAP  :: Object instances, arrays, class members,
           Dynamic (at runtime)
           Garbage Collector
           Shared across threads


 */
public class mgmt {
    public static void main(String[] args) {

    }
}
