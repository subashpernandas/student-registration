spring.application.name=basic-codes

server.port=${PORT:0}
spring.datasource.url=jdbc:mysql://localhost:3306/employee_test_db_one?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Force Spring to load SQL
#spring.sql.init.mode=always
#spring.sql.init.data-locations=classpath:test-data.sql

spring.jpa.generate-ddl=true
logging.level.org.hibernate.tool.hbm2ddl=DEBUG

app.name= MySpringApp
app.security.enabled=true
app.security.role= ADMIN

payment.mode=PAYMENT_MODE_FROM_LOCAL
